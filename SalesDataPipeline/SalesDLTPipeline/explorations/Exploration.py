# Databricks notebook source
# MAGIC %sql
# MAGIC SELECT * FROM workspace.sales.dim_customers;
# MAGIC -- SELECT * FROM workspace.sales.dim_products;
# MAGIC -- SELECT * FROM workspace.sales.fact_sales;
# MAGIC -- SELECT * FROM workspace.sales.mv_sales_by_region_and_categories;
# MAGIC -- SELECT * FROM workspace.sales.salesdltpipeline;
# MAGIC -- SELECT * FROM workspace.sales.silver_customers;
# MAGIC -- SELECT * FROM workspace.sales.silver_products;
# MAGIC -- SELECT * FROM workspace.sales.silver_sales;
# MAGIC -- SELECT * FROM workspace.sales.stg_customers;
# MAGIC -- SELECT * FROM workspace.sales.stg_products;
# MAGIC -- SELECT * FROM workspace.sales.stg_sales;
